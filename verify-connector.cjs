const fs=require('fs');const source=JSON.parse(fs.readFileSync('data/creative-daily.json','utf8'));const got=JSON.parse(fs.readFileSync('evidence/windsor-query-result.json','utf8'));const clean=got.map(r=>Object.fromEntries(Object.entries(r).map(([k,v])=>[k.replace(/^zuri_home_b8_31__/,''),v])));const key=r=>r.date+'|'+r.creative_id;const indexed=new Map(clean.map(r=>[key(r),r]));let checked=0;for(const r of source){const c=indexed.get(key(r));if(!c)throw Error('Missing row '+key(r));for(const [k,v]of Object.entries(r)){if(c[k]!==v)throw Error('Mismatch '+key(r)+' '+k);checked++;}}if(got.length!==84)throw Error('Wrong count');const report={verified_at:new Date().toISOString(),connector:'file',windsor_account_id:'368',dataset:'zuri_home_b8_31',authenticated_user:'zz215@expert.micro1.ai',source_rows:source.length,returned_rows:got.length,cells_compared:checked,all_values_match:true,method:'Read all discovered fields with Windsor get_data, then compare every source value by date plus creative ID',date_from:'2026-09-09',date_to:'2026-09-22'};fs.writeFileSync('evidence/connector-verification.json',JSON.stringify(report,null,2));console.log(JSON.stringify(report));
